package org.example;


public class Main {
    public static void main(String[] args) {
        // Chrome - ChromeDriver - Methods close()
        // WebDriver webDriver = new We
        // chromedriver.exe 를 직접 호출할 수 없기 때문에 Selenium은 내부적으로 webdriver.chrome.driver라는 키 값을 검색한다
//      SelIntroduction selIntroduction = new SelIntroduction();
        Locators locators = new Locators();



    }
}